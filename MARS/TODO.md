# TODO - Multiagent run + PDF output (LangChain vs LangGraph/LLM)

- [ ] Inspect existing reports in `MARS/outputs` to determine naming and format
- [ ] Draft run command for topic that compares LangChain vs LangGraph (or existing topic used in prior report)
- [ ] Execute `python main.py` with appropriate topic and ensure PDF is generated
- [ ] Verify generated PDF exists under `MARS/outputs/`
- [ ] If PDF generation fails, inspect logs and adjust/patch `pdf_export.py` or report agent
- [ ] Produce final summary referencing the generated markdown + pdf paths

